# Local-only reference material — do not publish. Root-anchored so it does NOT
# match skill/reference/ (the shipped on-demand skill docs).
/reference/
# generated by install.sh (machine-specific absolute paths)
host/native-host
node_modules/

# extension signing private key — never publish
host/extension-private-key.pem
