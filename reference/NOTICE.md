# Provenance / license notice

The files in `codex-chrome/` are copied verbatim from OpenAI Codex's bundled Chrome
plugin on this machine (`~/.codex/plugins/cache/openai-bundled/chrome/`). They are
**OpenAI proprietary** (`plugin.json` license: "Proprietary") and kept here only as a
local reference for porting the *doctrine* into our own `skill/SKILL.md`.

Do NOT publish or redistribute this directory. It is gitignored for that reason.
Our own skill (`skill/SKILL.md`) is a clean-room rewrite in our words.
